This script is used to collect filter settings passed via the url and uses it to retrieve the appropiate textbooks. It provides the main functionality for the textbook list page and is responsible for not only collecting filtered results. But also is responsible for collecting all the results for the textbookpage. The script is utilized by the textbook listing page and stores its results in a varible.