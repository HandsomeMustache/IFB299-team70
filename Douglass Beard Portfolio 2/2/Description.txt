This script utilizes the script showed in the 1st artifact. Once that script has been run this script collects the results and prints them out using an echo function in php. the script each result is printed with the appropiate links so the user it to browse books and vist their appropiate page. It also prints which page user is viewing and contains buttons to navigate results. These buttons are dynamic and will only display when appropiate.