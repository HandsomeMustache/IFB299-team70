This script ustilizes php mailer to handle lost passwords. If the user forgets their password they can make a request with their username and this script will find the associated email, generate a validation code, store it in the database and email to the user. This provides the user with some back options incase they forget their password and automates the process of identification and alteration of the users account.